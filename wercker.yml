box: node

build:
  steps:
    - npm-install
    - script:
        name: running gulp default task
        code: ./node_modules/.bin/gulp

deploy:
  steps:
    - git-push:
         gh_oauth: $GH_TOKEN
         gh_pages: true
         gh_pages_domain: mitevs.github.com
         basedir: dist