box: node

build:
  steps:
    - npm-install
    - script:
        name: running build
        code: npm run build 

deploy:
  steps:
    - leipert/git-push:
         gh_oauth: $GH_TOKEN
         gh_pages: true
         basedir: dist