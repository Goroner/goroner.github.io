box: node

build:
  steps:
    - npm-install
    - script:
        name: running webpack
        code: ./node_modules/.bin/webpack

deploy:
  steps:
    - leipert/git-push:
         gh_oauth: $GH_TOKEN
         gh_pages: true
         basedir: dist